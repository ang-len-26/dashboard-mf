import ReactDOM from "react-dom/client";
import AuthModule from "./pages/AuthModule";

const root = ReactDOM.createRoot(
  document.getElementById("root") as HTMLElement
);

root.render(<AuthModule />);
